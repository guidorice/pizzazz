// small: map controls,thumbnails
export const small = _ => ({
    boxShadow: '0 1px 2px 0 rgba(0, 0, 0, 0.40)',
});

// medium: dropdown menus panels
export const medium = _ => ({
    boxShadow: '0 2px 10px 0 rgba(0, 0, 0, 0.30)',
});

// large: modals, dialogs
export const large = _ => ({
    boxShadow: '0 5px 20px 0 rgba(0, 0, 0, 0.20)',
});
